---
const { user } = Astro.locals;

if (!user) {
  return Astro.redirect('/auth/login');
}

if (user.role === 'STAFF') {
  return Astro.redirect('/dashboard/pos');
}

return Astro.redirect('/dashboard/overview');
---
